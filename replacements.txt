
# This file must NOT contain the leaked secret in the repository.
# Create a local replacements file before running git-filter-repo, for example:
#
#   echo "PASTE_LEAKED_SECRET==>[REDACTED_GROQ_API_KEY]" > local-replacements.txt
#   git filter-repo --replace-text local-replacements.txt
#
# The repository's tracked `replacements.txt` is intentionally left empty to avoid
# storing secrets in source control.

